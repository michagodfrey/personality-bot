{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tailwind": "npx tailwindcss -i ./style/input.css -o output.css --watch",
    "minify": "tailwindcss -i ./style/input.css -o output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.1",
    "vite": "^4.2.0"
  }
}
